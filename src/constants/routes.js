const home = "/";
const logIn = "/log-in";
const signUp = "/sign-up";
const movies = "/movies";
const favorites = "/movies/favorites";
const detailsPage = "/movies/:movieID";
const notFound = "*";

const routes = {
  home,
  signUp,
  logIn,
  movies,
  detailsPage,
  favorites,
  notFound,
};

export default routes;
